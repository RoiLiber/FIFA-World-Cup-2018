import { combineReducers, createStore } from "redux";


const startingState = {};


function appReducer(state, action){
    
  return state;
}

const store = createStore(appReducer, startingState);

export default store;